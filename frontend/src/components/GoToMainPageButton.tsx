import { Button } from "@mui/material";

function GoToMainPageButton() {
  return (
    <Button variant="contained" color="primary" href="/" sx={{ my: 2 }}>
      Go to main page
    </Button>
  );
}

export default GoToMainPageButton;
