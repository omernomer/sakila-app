{
  "name": "sakila-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "fe": "npm run dev --workspace=frontend",
    "be": "npm run dev --workspace=backend",
    "db": "npm run db:up --workspace=backend"
  }
}
